apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zeus.fullname" . }}-config
  labels:
    {{- include "zeus.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
